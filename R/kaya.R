full_kaya_comparison <- function(past_kaya_no_emissions,past_proj,total_gross_emissions,data_long_clean) {
  
  # pulls in the past br kaya stuff for energy, gdp, population from past_kaya_no_emissions
  # adds the total gross emissions variable from past_proj
  # adds the projected total gross emissions from total_gross_emissions
  # adds the projected energy from data_long_clean as a range ("Final Energy")
  # adds the projected gdp and pop from OP-NEMS (data_long_clean)
  # copies the historical energy, gdp, population from the br5 scenario to btr1 scenario
  # calculates the kaya identity variables
  
  ### Then tar_load() this target in btr_tables_figs and create the figure there
  
}